language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ugrunt' ]; then cd .. && eval "mv $currentfolder generator-ugrunt" && cd generator-ugrunt; fi

